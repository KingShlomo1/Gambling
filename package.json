{
  "name": "wealth-agent",
  "private": true,
  "scripts": {
    "dev:worker": "cd worker && wrangler dev",
    "dev:frontend": "cd frontend && pnpm dev",
    "deploy:worker": "cd worker && wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply wealth-agent-db --local",
    "migrate:remote": "wrangler d1 migrations apply wealth-agent-db --remote",
    "typecheck": "pnpm -r typecheck"
  }
}
